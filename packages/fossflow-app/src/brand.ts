/**
 * Central display-only product identity for the application UI.
 *
 * This is branding copy for the first formal release identity of the fork.
 * Do not derive it from the synchronized package.json versions (see
 * `npm run update-version`) and do not use it for model formats, repos,
 * tags, or npm metadata. Technical package versions are managed separately
 * during final release prep.
 */
export const APP_DISPLAY_NAME = 'FossFLOW 2.5D';
export const APP_DISPLAY_VERSION = '1.0.0';
export const APP_DISPLAY_IDENTITY =
  `${APP_DISPLAY_NAME} v${APP_DISPLAY_VERSION}`;

/** DOM id of the header slot that hosts the MainMenu trigger button. */
export const MAIN_MENU_SLOT_ID = 'fossflow-mainmenu-slot';
